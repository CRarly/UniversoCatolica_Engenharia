import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "br.edu.ucb.desafioestrutural",
  appName: "Desafio Estrutural",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
  backgroundColor: "#060919",
};

export default config;
